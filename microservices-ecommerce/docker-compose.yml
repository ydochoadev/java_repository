services:
  # 1. El Servicio de Base de Datos
  mongodb:
    image: mongo:7.0.4
    container_name: mongodb
    ports:
      - "27017:27017"
    environment:
      - MONGO_INITDB_ROOT_USERNAME=root
      - MONGO_INITDB_ROOT_PASSWORD=password
    volumes:
      - mongo_data:/data/db  # <--- ¡Aquí salvamos los datos!

  inventory-db:
    image: mysql:8
    container_name: inventory-db
    ports:
      - "3306:3306"
    environment:
      MYSQL_ROOT_PASSWORD: root
      MYSQL_DATABASE: inventory-db
    volumes:
      - inventory_data:/var/lib/mysql

  order-db:
    image: postgres:16-alpine
    container_name: order-db
    ports:
      - "5432:5432"
    environment:
      POSTGRES_USER: admin
      POSTGRES_PASSWORD: admin
      POSTGRES_DB: order-db
    volumes:
      - order_data:/var/lib/postgresql/data

  keycloak-db:
    image: postgres:16-alpine
    container_name: keycloak-db
    ports:
      - "5433:5432"
    environment:
      POSTGRES_USER: keycloak
      POSTGRES_PASSWORD: password
      POSTGRES_DB: keycloak-db
    volumes:
      - keycloak_data:/var/lib/postgresql/data

  # 2. Keycloak Service
  keycloak:
    image: quay.io/keycloak/keycloak:24.0.1
    container_name: keycloak
    command: start-dev # Modo desarrollo (rápido y sin HTTPS obligatorio)
    environment:
      KC_DB: postgres
      KC_DB_URL: jdbc:postgresql://keycloak-db:5432/keycloak-db
      KC_DB_USERNAME: keycloak
      KC_DB_PASSWORD: password
      # Credenciales del Super Admin (Para entrar a la consola ya mismo)
      KEYCLOAK_ADMIN: admin
      KEYCLOAK_ADMIN_PASSWORD: admin
    ports:
      - "8080:8080"
    depends_on:
      - keycloak-db

  # RabbitMQ
  rabbitmq:
    image: rabbitmq:4.2-management  # Version
    container_name: rabbitmq        # Nombre del contenedor (docker desktop)
    ports:
      - "5672:5672"   # Puerto para comunicación entre microservicios
      - "15672:15672" # Puerto para la consola de gestión web
    environment:
      RABBITMQ_DEFAULT_USER: guest
      RABBITMQ_DEFAULT_PASS: guest
    volumes:
      - rabbitmq_data:/var/lib/rabbitmq # <--- ¡La persistencia!

  # Stack Grafana para los logs, trazas y métricas
  grafana-lgtm:
    image: 'grafana/otel-lgtm:latest'
    ports:
      - '3000:3000' # ventana visual, navegador, interfaz de grafana
      - '4317:4317' # Grpc, puerto en alto rendimiento en producción
      - '4318:4318' # Http
    environment:
      - OTEL_METRIC_EXPORT_INTERVAL=5000  # métricas se envían a grafana cada 5s

volumes:
  mongo_data:             # Declaramos el volumen persistente para product
  inventory_data:         # Declaramos el volumen persistente para inventory
  order_data:             # Declaramos el volumen persistente para orders
  keycloak_data:          # Declaramos el volumen persistente para la seguridad
  rabbitmq_data:          # Declaramos el volumen persistente para rabbitMQ

#  # 2. Nuestra Aplicación (Product Service)
#  product-service:
#    build: .              # Usa el Dockerfile de la carpeta actual
#    container_name: product-service
#    ports:
#      - "8080:8080"
#    environment:
#      # Sobrescribimos la config de Spring para usar Docker
#      # Fíjate: usamos 'mongodb' en lugar de 'localhost'
#      - SPRING_DATA_MONGODB_URI=mongodb://root:password@mongodb:27017/product-db?authSource=admin
#    depends_on:
#      - mongodb           # Espera a que la BD arranque primero